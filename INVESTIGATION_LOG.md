# Go2 ブラインド階段登坂ポリシー: 調査・改善ログ

auto_tune_loop.shによる貪欲法チューニングから、反転登坂エクスプロイトの発見・修正、
歩容協調の改善までの経緯。各節は仮説→対応→結果の順。

---

## 1. auto_tune_loop.sh時代: 貪欲法での報酬チューニング

`scripts/auto_tune_loop.sh`で「直近のBESTから1つ変更→学習→比較→採用/不採用」を繰り返し、
terrain_levels > 6.0を目指した。

**交絡問題**: 貪欲法だと採用した変更が単体で効く主効果か、既存の積み上げとの交互作用かが
区別できないというユーザー指摘を受け、採用時は共通祖先(BASE)からも単体確認runを実施する
方式に変更。

**主な採用済み変更**: target_clearance 0.08→0.15、joint_pos weight -0.4→-0.2、
flat_orientation_l2 -1.0→-0.5(採用、terrain_levels 5.427)。forward_command_progress
1.5→2.2は探索が広がりすぎて不採用。

**demote_fraction緩和(level3限定 0.5→0.25)**: 階段では同じコマンド速度でも正味変位が
小さいため、標準の降格閾値では真に前進していたロボットも降格してしまうと判断し緩和。
terrain_levels 4.56→5.12に改善し、以後のBESTの土台になった(**後日§4.1で撤回**)。

---

## 2. mujoco階段検証と地形改修

インターン先の先輩から、mujocoで階段幅を27cm→20cmにしたところgo2の前足が接触した瞬間
フリーズする現象が報告された。学習側の階段は`step_height_range`(高さ)はランダム化されて
いるが`step_width`(踏面奥行き)は常に0.3固定で、20cm幅は分布外(OOD)の可能性が高いと指摘。

mujoco地形は、既存のオブジェクト・坂を異なる寸法の階段に、スケルトン階段を通常階段に変更し、
4種類の階段を1シーンに同居させて比較できるようにした。

---

## 3. 反転登坂エクスプロイトの発見

mujoco検証中、直進コマンドのみでも階段を数段登った所で**体を反転させ、後ろ足側を先導させて
登り続ける**挙動が発覚(高さ20cm/幅20cmで顕著)。

**原因**: `forward_command_progress`/`track_lin_vel_xy_exp`はボディ座標系の前進速度しか
見ておらず、これは数学的に「世界座標系の速度と体の向きの内積」と等価なため、体がどちらを
向いていようと区別がつかない。コマンド生成側も向きを一切拘束していなかったため、反転して
後ろ足で前進を満たす戦略が報酬上まったく区別なく成立していた。terrain_levelsの昇格判定も
向きを見ないため、この抜け穴を検知できない。

---

## 4. heading_command導入(Try-1〜4)とその限界

### 4.1 先輩方式への移行

インターン先の先輩の`train-and-check`スキルと比較し、運用を変更した:
- demote_fractionを標準0.5に復元(§1の変更を撤回)。カリキュラム判定ロジック自体は以後変更しない。
- 各試行の起点をPhase2チェックポイント(`2026-07-04_05-26-55/model_6998.pt`)に固定し、
  貪欲法をやめる。
- `go2/sandbox/`配下に試行ごとの新規config+`gym.register`(戦略コメント付き)を作る方式
  (`Unitree-Go2-Velocity-v1-Phase3-Try-N`)に変更。

**heading_commandの導入**: `CommandsCfg.base_velocity`にIsaacLab標準の`heading_command=True`
を追加。目標の向きとの誤差から旋回コマンドを常時自動生成し、反転して向きを保持する行動に
継続的な減点がかかるようにした。

### 4.2 Try-1〜4の結果

| Try | 変更 | terrain_levels | bad_orientation | 備考 |
|---|---|---|---|---|
| Try-1 | heading_command=True | 6.3〜6.4 | 13〜15%(旧比100倍) | 目標達成、転倒率悪化 |
| Try-3 | step_width 0.3→0.2 | 5.4〜5.5 | 27〜30% | 幅20cmより広い階段への汎化は確認、20cm自体は未解決 |
| Try-4 | rel_heading_envs 1.0→0.3 | 6.1 | 12% | 指標は良好だが実機では未改善 |

**Try-4の実機検証で判明**: mujocoで試したユーザーから「特に何も改善していない。横移動が
できない(脚は動くが1歩も進めない)。通常歩行も悪化した」との報告。**tensorboard上の集計指標は
この問題を全く検出できていなかった**。

<video controls src="https://chiho-api.openreach.tech/employee/v1/files/UPLFILEZZO3onfgNl8QAtS4nuhOTTrKjbDzC2SX"></video>

---

## 5. heading_command撤回、報酬関数のみのアプローチへ

### 5.1 heading_drift_penaltyの設計

heading_commandを完全撤回(コマンド生成は元の自由サンプリングに戻す)。代わりに
`mdp.heading_drift_penalty`という報酬関数を新設: スポーン時の向きから、コマンドされた
旋回速度を毎ステップ積分した「期待される向き」を追跡し、実際の向きとの差を二乗ペナルティに
する。正当な旋回コマンドには追従しても減点なし、コマンドされていない反転だけが持続的に
減点される設計。weight=-0.3は暫定値。

### 5.2 Try-5: 重大バグ

**結果**: terrain_levels 4.5(全Try中最低)、bad_orientation 47%。

**原因**: IsaacLabでは`episode_length_buf`がreward計算の**前に**インクリメントされ、reset
による0初期化は計算の**後**に行われる。そのため実装で使っていた`episode_length_buf == 0`
という「リセット直後」判定が一度も真にならず、期待される向きがエピソードをまたいでリセット
されず、無関係な値と比較され続けて理不尽に減点していた。

**修正**: 判定条件を`episode_length_buf == 1`に変更。

### 5.3 Try-6: バグ修正後の健全な結果

**結果**: terrain_levels 5.87、bad_orientation 15.2%(Try-1相当まで回復)、entropy健全。

**mujoco実機検証**: 前足はしっかり次の段に足を乗せられるようになった。ただし**後ろ脚が
追いつかず「バタバタ」してしまう**という新たな歩容協調の問題が判明。

---

## 6. 歩容協調の改善(Try-7)

ユーザーの観察と着想: 「前足は次の段に乗るが後ろ脚が追いつかない。犬の階段の登り方が
ヒントにならないか」。

実際の四足動物は階段など不安定な足場では、対角線ペアが同時に動くトロットではなく、
**一度に1本ずつ脚を振り出し常に3本を接地させる「側方歩様」**に切り替えることが知られている。
コードベースに未使用のまま存在していた`mdp.feet_gait`(位相クロック式の歩容報酬)を使い、
脚順序RR→FR→RL→FL(0.25ずつ位相をずらす)、stance比率75%、weight=0.2で追加。
period/weightは根拠となる実測値がまだ無い暫定値。

**結果**: terrain_levels 5.85(Try-6の5.87とほぼ同水準、歩容制約を追加しても登坂性能は
落ちない)、bad_orientation 10.2〜10.4%(Try-6の15.2%から改善、Try-4の12%を上回り全Try中
最良)、entropy 7.68(健全)。feet_gait報酬は0.34〜0.36/stepで安定推移し、脚間の位相協調を
実際に学習していることを確認。export・mujoco配備済み。後ろ脚の「バタバタ」解消と横移動の
健全性は実機検証待ち。

---

## 7. 片脚リフト力不足の発見と2つの仮説(Try-8/9)

**mujoco実機検証で判明**: 1段目と2段目に前脚を1本ずつ置いた状態から、高い方の脚だけで
体を持ち上げられない。段差が浅く踏面が広い階段では問題ないが、高くて踏面の狭い階段では
これが原因で後ろ脚が1段目に届かない。Try-7の`feet_gait`はタイミング調整のみで、この
力不足そのものには効かなかった。

原因調査: アクチュエータのトルク上限は実機Go2の値(`effort_limit=23.5Nm`)に厳密に
合わせてあり、ここを緩めるのは実機で再現できない解決策になるため対象外。一方、既存の
報酬には「登坂中に体を持ち上げること」自体を直接後押しする項が無いと判明
(`forward_command_progress`は水平速度のみ、`feet_gait`はタイミングのみ)。

2つの仮説をTry-7から独立に分岐して検証:
- **仮説A(Try-8)**: `body_height_gain` — 体幹の絶対高さの増分(dz/dt)を直接報酬。
- **仮説B(Try-9)**: `foot_touchdown_height_gain` — 各脚の接地高さがそのepisode内での
  自己ベストを更新した瞬間だけ報酬。

### 7.1 Try-8(仮説A): 却下

**tensorboard**: terrain_levels 5.99、bad_orientation 9.1〜9.2%(全Try中最良)、entropy
7.85(健全)—指標だけを見ると最良の結果に見えた。

**mujoco実機検証で判明**: ほとんど動けなくなった。**静止することが最適解になってしまった**。

**原因**: `body_height_gain`はzの増分が**正のときだけ**報酬し、負の増分はクランプでゼロに
していた。接触・物理演算の微小なノイズによる上下動でも、上がった瞬間だけ拾われて報酬が
積み上がり、下がった分は無罰。これは`foot_clearance_terrain_adaptive`のdocstringに残る
「`exp(-penalty)`形式は棒立ちが満点になり得る」という過去の教訓と同じ構造の抜け穴を、
別の形で再び作り込んでしまったもの。4096並列環境の集計では「動く方が他の報酬込みで得」
なため学習中は隠れていたが、実機単体展開でこの抜け穴が顕在化した。仮説Aは撤回。

### 7.2 Try-9(仮説B): 効果はほぼ無かった可能性

**tensorboard**: terrain_levels 5.77(Try-7の5.85と同水準)、bad_orientation 7.9〜8.1%
(全Try中最良)、entropy 7.67(健全)。

**しかし**、`Episode_Reward/foot_touchdown_height_gain`自体は0.0001〜0.0002/stepしかなく、
同じweight=1.0の`feet_gait`(~0.35/step)と比べて3桁小さい。自己ベスト更新イベントは
階段区間でしか起きずそもそも稀なうえ、平地・凹凸・箱地形も含む episode 全体の平均に
薄められてほぼゼロになっていたと見られる。Try-8のような抜け穴は無かったが、学習に
実質的な影響をほぼ与えられなかった可能性が高く、**この結果だけでは仮説Bの当否を
判断できない**。export・mujoco配備済み。

---

## 8. Try-3の`step_width`が引き継がれていなかったことに気づく

Try-4以降の全て(Try-4〜9)は「Try-1(=step_width 0.3固定)からの分岐」として設計されて
おり、Try-3だけの`step_width=0.2`は以後のTryに一切引き継がれていなかった、と後から
判明。つまりTry-5〜9で積み重ねてきた報酬改善(heading_drift_penalty、feet_gait、
リフト力系)は、すべて**実機階段(幅20cm)とは異なる分布(幅30cm)の上でしか検証されて
いない**ことになる。

改めてTry-3(唯一のstep_width=0.2版、報酬はTry-1相当のみ)を実機で見直したところ、
tensorboard指標は全Try中最下位(terrain_levels 5.4〜5.5、bad_orientation 27〜30%)
にもかかわらず、**実機での挙動は現時点で最も良いかもしれない**という所見。これは
Try-3自身の想定(「これは報酬設計の問題ではなく分布網羅の問題」)と一致しており、
報酬の工夫を積み重ねるより学習地形を実機に合わせることの方が本質的だった可能性が高い。

**次の一手**: Try-3の`step_width=0.2`とTry-7の報酬スタック(feet_gait +
heading_drift_penalty)を組み合わせたTry-10が、まだ誰も試していない組み合わせとして
有力候補。

---

## 全Try比較表

| Try | 変更内容 | terrain_levels | bad_orientation | 備考 |
|---|---|---|---|---|
| 旧BEST(貪欲法) | demote_fraction=0.25 + 積み上げ報酬 | 5.427 | 0.15%(反転込みの可能性) | 系譜の起点 |
| Try-1 | Phase2起点、heading_command=True | 6.3〜6.4 | 13〜15% | 目標達成、転倒率悪化 |
| Try-2 | heading_control_stiffness 1.0→0.5 | 中断・未完走 | - | Try-3優先で中断 |
| Try-3 | step_width 0.3→0.2 | 5.4〜5.5 | 27〜30% | 指標最下位だが**実機は現時点で最良の可能性** |
| Try-4 | rel_heading_envs 1.0→0.3 | 6.1 | 12% | 指標は良好だが実機は未改善 |
| Try-5 | heading_drift_penalty追加 | 4.5 | 47% | **実装バグ** |
| Try-6 | Try-5のバグ修正版 | 5.87 | 15.2% | 健全。後脚の遅れが新課題 |
| Try-7 | + feet_gait(側方歩様) | 5.85 | 10.2〜10.4% | 全Try中bad_orientation最良 |
| Try-8 | + body_height_gain(仮説A) | 5.99 | 9.1〜9.2% | 指標は最良だが**実機は静止が最適解化** |
| Try-9 | + foot_touchdown_height_gain(仮説B) | 5.77 | 7.9〜8.1% | 抜け穴は無いが報酬がほぼ発火せず未検証に近い |

## 今後の課題

- **Try-10(最優先)**: Try-3のstep_width=0.2 + Try-7の報酬スタックの組み合わせを検証。
- Try-9(foot_touchdown_height_gain)は発火頻度が低すぎて仮説Bを実質検証できていない。
  重みを大きくするか、報酬設計自体を見直す必要あり。
- Try-7(feet_gait)のmujoco実機検証(後脚の「バタバタ」解消、横移動の健全性)は未確認のまま。
- Try-2(heading_control_stiffness)は未検証のまま。
- 幅20cmの階段固有の問題は、step_widthのランダム化(配分の設計が必要)で再挑戦する余地あり。
