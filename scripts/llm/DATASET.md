# データセットの作り方（2026-09-22 改訂）

形式・文法・工程がすべて変わったので旧版は捨てた。差分ではなく、いまの姿だけを書く。

## 0. 方針

**LLM は口語をポリシー命令に翻訳するだけ。** できるかどうかはポリシーの責務で、LLM の責務ではない。

この一行から次が従う:

- シミュレータでの検証で**ふるい落とさない**。サンプラーが組んだ分布をそのまま使う
- システムプロンプトにポリシー都合の制約（上限、成功率、技と方向の対応）を**書かない**
- 実行できない指示はコンパイラが吸収し、`adjustments` に記録して conductor が表示する。
  そのログはポリシー改善の材料であって、モデルを縛る材料ではない
- 返事に所要時間を書かない。モデルが計算できない数字を言わせない

## 1. 工程

```
sample_programs.py   programs.jsonl   {"id", "program"} だけ。4000本
        ↓ id のハッシュで半分に割る（is_templated）
        ├─ テンプレート側 ─→ phrasebank / dialogues が日本語を着せる
        └─ 手書き側 ──────→ LLM が会話ごと書く → handwritten.jsonl
                                  ↑ ここに人の目視ゲート（HANDWRITTEN.md §7）
        ↓
build_dataset.py     dataset.jsonl    会話。学習に使うのはこれだけ
        ↓
検査 → 学習 → 評価 → GGUF
```

割り方は id のハッシュ（`is_templated`）で、シードにもファイル順にも本数にも依らない。1本のプログラムが
両側に現れたり、どちらにも現れなかったりしないため。

`validate_programs.py`（Isaac Sim, 2時間）は**データ生成の工程から外れた**。ポリシーの実力を測るためには今も回すが、データセットは通らない。校正表（距離⇄時間）だけは `--calibrate` で作り、コンパイラが使う。

## 2. 1行の作り方

```
プログラム      programs.jsonl から
口調           polite / kansai / short から1つ
開始キュー      待機中なら []、実行中なら「そのとき残っている手順」
指示文         プログラム → 日本語
返事           指示の言い直し＋相槌
目標プログラム   開始キューと指示から決まる列
```

学習テキストはこうなる（`chat_format`）:

```
<|im_start|>user
{人の発言}

queued programs: {開始キュー}<|im_end|>
<|im_start|>assistant
{返事}

program: {目標}<|im_end|>
```

履歴は会話だけ。`queued programs` も `program:` も、過去ターンには残さない。

## 3. 多ターンの作り方 ― 2つの切り口

1本のプログラムを**どこで切るか**で会話を作る。シミュレータは使わない。

### 3.1 時刻で切る（実行中に話しかける）

コンパイル済みタイムライン上の時刻 `t` を選び、そこで割り込む。開始キューは「`t` 時点の残り」で、
済んだ手順は消し、途中の手順は残量に縮め、未着手はそのまま渡す。

```
[右側転, 左回90°, 遅後3s]  を t=3.0s で切る
  → queued programs: [{turn left 38.28°}, {move backward slow 3.0s}]
```

ここから作れる会話: 割り込み / 停止 / 追加 / 訂正 / 雑談 / 状態確認。

### 3.2 手順で切る（待機中に順に指示する）

プログラムを手順の境目で分け、1ターンに1つ（または数個）ずつ指示させる。各ターンの開始キューは
`[]`（前のターンが完走している）。**正解は元のプログラムから機械的に決まる。**

```
[バク転, バク転, バク転, 倒立8s]
  ターン1  「バク転して」          → [バク転]
  ターン2  「もう一回」            → [バク転]        ← 直前を参照
  ターン3  「あと1回、そのあと倒立8秒」→ [バク転, 倒立8s]
```

**同じ手順が連続する箇所**（4000本中399本、すべて技）が「もう一回」の素材になる。
**同じ技が離れて出てくる箇所**が「2つ前の技」の素材になる。

```
[バク転, 前3m, バク転]
  ターン1  「バク転して」      → [バク転]
  ターン2  「前に3m」          → [前3m]
  ターン3  「2つ前の技もう一回」 → [バク転]
```

参照が解けるのは、**履歴に人の発言がそのまま残っている**ため。「バク転して」という自分の言葉を
数えて遡ればよく、返事の文面にも program 行にも依存しない。実運用で多用される形なので、
この軸は待機中データの主力にする。

参照の言い方（表に持つ）:

```
直前      もう一回 / もっかい / さっきのやつ / 同じの / リピート
2つ前     2つ前の技 / その前のやつ / 2個前の
名前指し   さっきのバク転 / 例の倒立 / 最初のやつ
変更つき   さっきのやつ、今度はゆっくり / 2つ前のを2回
```

## 4. 言い回し ― 手書きとテンプレートの混成

今日までに分かったのは、**テンプレートの表層が狭いことが失敗の唯一の原因**だったということ
（「とまれ」がひらがなで0件、語尾が「ください」偏重、語順が固定、複数手順を1つに畳めない）。

そこでプログラムを半分に割る:

```
2035本  手書き        LLM が会話ごと書く。書き方の仕様は HANDWRITTEN.md
1964本  テンプレート   phrasebank の合成。単発と、dialogues.py の多ターン
```

**手書きの単位は1文ではなく1会話**（2〜5ターン、単発は無し）。1文で書かせた版は失敗して捨てた
― 2036本書いて、文末「〜ください」が19%、「〜て、〜て」連結が52%、言い直しは0.3%。合成側の分布の
コピーやった。原因は**検査を書く工程の中に置いたこと**で、`parse_instruction.py`（合成側の語彙で
書かれたパーサ）を通すよう直させた結果、通る文＝合成と同じ文に収束した。

いまの手書き工程はこう変えてある:

- **書くときは検査を一切走らせない。** 生の出力をそのまま集める。構造検査は後工程（下記）
- 単位は会話。口語が現れるのは2ターン目以降 ―「もうええって」「ちゃうちゃう逆」「さっきのやつもっかい」
  ― 先行文脈があって初めて代名詞・省略・言い直しが自然に書ける
- 10本ずつのバッチに**要求特性を機械生成して添付**する（言い直しの必須件数、このバッチで禁止する
  語尾、必ず入れる会話の型、ターン数の配分）。バッチ間・エージェント間の同質化を防ぐため
- 返事も手書き側は LLM が書く。ただしロボットは**実行結果・成否・得手不得手・所要時間を語らない**
  （結果を知る経路が無いし、できるかどうかはポリシーの責務）

### 慣用句セット（実装済み ― `phrasebank.py` 末尾）

合成は「方向語＋速度語＋量＋動詞」しか書けない。つまり `左に180度回って` は書けても `回れ右!`
は永遠に出てこない。そこで**プログラムの形**にマッチする慣用句表を用意した。

```
turn              1168回  回れ右 / 後ろ向いて・振り向いて・反転・Uターン・半回転 / 左向け左
                          左に曲がって・左折 / 一周・一回転 / 4分の3周 / 斜め左 / ちょっとだけ左
move∞              649本  前に進んで / ずっと前に / 止めるまで歩いてて / 歩き続けて / ストップと言うまで
move→flip→move∞    419本  走りながら{技}して、そのまま走ってて / 3秒走ってから{技}、あとはずっと走ってて
flip               168本  {技}1発! / {技}いったって! / {技}決めて! / {技}いこか!
flip×N             190本  {技}2回 / {技}2連発 / {技}、もういっちょ / 連続で{技}3回
move→flip          132本  助走つけて{技} / 3m進んでから、止まらずに{技}
stance∞             41本  ずっと立ってて / 倒立したままでいて / 倒立キープ
move / stance      539本  3mだけ前に / 前に3mお願いします / 5秒だけ逆立ち / 倒立3秒キープ
2手の組み合わせ     380本  回れ右して3m / 3m進んでから左に曲がって / {技}してから前に3m / 3m進んで倒立5秒
```

プログラムの53%がどれかの表に当たる。`IDIOM_RATE = 0.75`（当たった形のうち慣用句で書く割合）で、
**指示文全体の50%が慣用句**になる。`build_dataset.py` が実測値を出力する。

言い切らない言い回しが2つだけある。`走りながら{技}`（助走の長さを言わない）と `少し左向いて`
（角度を言わない）で、**読み手が補う値（3秒・30度）のときだけ**描く。往復パーサが同じ既定値で
補うので、検査は素通りせず成立する。

左右を言わない `後ろ向いて` `一周` は `dir: "any"` として読み返し、比較はどちらの向きも通す。
日本語として本当にどちらでも満足するから。

### 検証 ― 側によって別のものを見る

| | かけるもの | 見るもの | 現状 |
|---|---|---|---|
| テンプレート側 | `parse_instruction.py` → `roundtrip_check.py` | 指示文がプログラムと同じことを言っているか | **5380/5380 通過** |
| 手書き側 | `check_handwritten.py` | 行の形・program の文法・queue の連続性。**日本語は読まない** | **2035/2035 通過** |
| 両方 | `show_handwritten.py --stats` | 分布（文末の偏り・軸のカバー率・重複） | 人が見る |

往復にかけるのは「停止状態で指示文まるごと1つ」のターンだけで、`roundtrip: true` が付く。
**手書き側には付けない** ― 自然に書くほど合成側のパーサは読めなくなるので、読めることを条件にすると
上に書いた失敗をそのまま繰り返す。代わりに構造だけを機械で見て、言い回しは人が分布で見る。

構造検査は既存のバグも1つ見つけた: `sc_append` が、終わりのない `move` が走っている最中に
「終わったら〜も」を足して `[長さ無しmove, 追加手順]` という文法違反のプログラムを作っていた
（dlg_append 320本中93本）。そのプログラムでは追加シナリオを組まないように直した。

## 5. 決まっていること

| | |
|---|---|
| 文法 | move / turn / flip / stance の4スキル。stop と jump は廃止、斜め4方向も廃止 |
| 長さ | move は 秒・距離・省略(=ずっと)、stance は 秒・省略(=ずっと)。省略は末尾のみ |
| 技 | 1ステップ1回転。2回は2ステップ。走り技かどうかは直前が move かで決まる |
| 旋回 | 角度のみ、速度は常に最速 |
| 出力 | 返事 + `program:`。action 行は無し。返した列がそのまま新しい予定 |
| 履歴 | 会話だけ。10メッセージ（5往復） |
| think | どこにも入れない。GBNF が返事の `<` 始まりを禁止 |
| 規模 | 6251行 / 14720ターン。手書き会話2035 ＋ 対話3200 ＋ 単発516 ＋ 否定500。会話が行の84%・ターンの93% |

## 6. 残っていること

- サンプラーからポリシー都合を抜く（`running_pair` の方向と技の対応、`running_flip_speeds` の
  速度制限、`flip_run_up_s` の助走2〜3秒）。代わりにコンパイラが `min_run_up_s` などで吸収し、
  調整をログに出す
- **評価スクリプトが無い**。旧 `eval_model.py` は action / 警告 / 辞退を採点する旧設計だったので捨てた。
  いま要るのは「生成 → `parse_output` → `programs_match` でターンごとに突き合わせ、カテゴリ別に出す」だけ
- 学習 → 評価 → GGUF 書き出し → MuJoCo で実機手前の確認

## 7. 工程の入口

| やること | 見るもの |
|---|---|
| 手書き会話を作る・作り直す | [HANDWRITTEN.md](HANDWRITTEN.md)（仕様と運用手順） |
| 合成側の語彙・慣用句を触る | [phrasebank.py](phrasebank.py) |
| 多ターンのシナリオを触る | [dialogues.py](dialogues.py) |
| 出来を見る | `show_handwritten.py --stats` / `check_handwritten.py` / `roundtrip_check.py` |
